# Slurm_formula_BioMedIA
Salt formula for Slurm on BioMedIA cluster

Need to `mkdir /srv/` and copy the three folders `salt`, `pillar`, and `www` (`www` is currently empty, you can create an empty folder as well) into `/srv/`.
